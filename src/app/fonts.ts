import { Inter } from "next/font/google";
export const fontSans = Inter({ subsets: ["latin"], variable: "--font-sans" });
export const fontHeading = Inter({ subsets: ["latin"], variable: "--font-heading" });
